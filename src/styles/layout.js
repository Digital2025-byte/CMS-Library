/**
 * Shared horizontal page shell — keep identical across sections
 * so left/right edges align on every breakpoint.
 */
export const pageContentShell =
  "mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-12";

/** Horizontal padding only (when max-width is handled elsewhere). */
export const pageContentPadding = "px-4 sm:px-6 lg:px-12";
