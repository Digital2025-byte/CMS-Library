/**
 * Help page — the components seed from their own demo data (which already
 * carries the help-page content), so plain block ids are enough.
 */
export const helpPageBlocks = [
  // The help hero is text-only in the source app — hide the demo image.
  { sectionId: "page-hero", style: { showImage: false } },
  "search-console",
  "help-categories",
  "cta-banner",
  "journey-section",
];
