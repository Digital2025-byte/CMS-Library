import {
  CookieIcon,
  FileTextIcon,
  ShieldIcon,
} from "@phosphor-icons/react";

export const LEGAL_ICON_MAP = {
  shield: ShieldIcon,
  cookie: CookieIcon,
  document: FileTextIcon,
};

export const DEFAULT_LEGAL_ICON = FileTextIcon;
